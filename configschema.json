{
    "type": "object",
    "properties": {
        "bot_username": {
            "type": "string",
            "description": "Username of the bot"
        },
        "bot_oauth": {
            "type": "string",
            "description": "oAuth token for the bot"
        },
        "channel": {
            "type": "string",
            "description": "The channel the bot should join to retrieve chat messages"
        }
    },
    "required": [
        "bot_username", 
        "bot_oauth", 
        "channel"
    ]
}